# 需求变更索引

Consultant 每次修改需求文档后在此追加一条。Manager 每次规划任务前先读此文件。

---

（尚无变更记录）

---

## 2026-04-14 — MC 认证真理源确认（EasyAuth + SQLite）

- 来源：用户提供当前 Fabric（服务端核心） 服务器的实际部署信息。
- 已确认事实：
  - 当前离线服认证模组为 EasyAuth（离线服认证模组），不是 AuthMe（认证模组）。
  - 认证存储不是 PostgreSQL（关系型数据库） / `authme schema`（认证模式），而是独立 SQLite（嵌入式数据库） 文件。
  - 配置文件：`/home/hcid274/MC_Fabric_WSL_Server/config/EasyAuth/storage.conf`
  - 库文件：`/home/hcid274/MC_Fabric_WSL_Server/EasyAuth/easyauth.db`
  - 表名：`easyauth`
  - `data` 字段保存 JSON（结构化字符串），其中 `password` 为 bcrypt（哈希） 形式，不能反推明文。
- 影响评估：
  - 当前 `T-010`（本地装配 / 启停骨架） 可继续，不需改握手。
  - `05_DATA_SPEC.md`（数据规格） 中“`authme schema`（认证模式） 只读”的既有假设与现实部署不一致，后续需改写为“外部认证源”表述。
  - 后续涉及 MC（Minecraft） 登录、部署与运行时接入的任务，必须明确“机器人持有自管明文密码并走游戏内登录流程”还是“增加只读认证状态适配器”，不得默认把 EasyAuth（离线服认证模组） 数据迁入 TS Core（TypeScript 单核心） 的 PostgreSQL（关系型数据库） 主业务库。
- 待后续确认：
  - 是否仅把 EasyAuth（离线服认证模组） 视为外部认证源并只做运行时登录适配；
  - 是否需要单独定义一个只读认证适配层，用于查询用户名 / 注册状态 / 最近认证信息；
  - 是否需要在部署文档中记录 EasyAuth（离线服认证模组） 的路径、库结构与运维注意事项。

---

## 2026-04-15 — T-023 必须达到 MC 上线女仆测试口径

- 来源：用户直接约束任务排序与验收目标。
- 已确认要求：
  - 当前批次继续按模块批量下发，但 `T-023`（任务二十三） 必须达到 MC（Minecraft，我的世界） 实际上线女仆测试口径。
  - `T-021`（任务二十一） 继续负责 `conversation`（对话） + `workers`（工作线程） + `interfaces`（接口边界） + `app`（应用装配） 的真实消息链路。
  - `T-022`（任务二十二） 必须负责 `sandbox`（沙箱） + `skills`（技能） + `runtime`（运行时） + `workers`（工作线程） 的最小执行链路，不能只停留在纯契约或占位。
  - `T-023`（任务二十三） 必须把 `app`（应用装配） + `runtime`（运行时） + `interfaces`（接口边界） + `workers`（工作线程） + `observation`（观测） 组装成可对真实 MC（Minecraft，我的世界） 服务器进行上线测试的入口。
- 影响评估：
  - `T-023`（任务二十三） 的验收不再只是“端到端 demo（演示） 计划态”，而是必须能执行一次实际 MC（Minecraft，我的世界） 上线测试：启动 TS Core（TypeScript 单核心） 真实运行入口、连接服务器、按 EasyAuth（离线服认证模组） 外部认证策略执行登录命令（如配置要求）、接收一条测试消息并产生可观测的回复 / 动作 / 回执。
  - `T-022`（任务二十二） 必须提前补齐 BotWorker（机器人工作线程） 消费 exec（执行） 队列、调用 BotActor（机器人执行代理） 单写者动作入口、以及至少一个低风险可测动作或技能路径，否则 `T-023`（任务二十三） 无法上线测试。
  - EasyAuth（离线服认证模组） 仍作为外部认证真理源；本变更不允许把 SQLite（嵌入式数据库） 认证数据迁入 PostgreSQL（关系型数据库），也不允许双写认证状态。
- 待后续确认：
  - `T-023`（任务二十三） 的实际测试账号、服务器地址、端口、EasyAuth（离线服认证模组） 明文密码通过本地环境变量或本地配置注入，不写入仓库。
