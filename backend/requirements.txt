# MC_Servant Backend Dependencies

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
websockets>=12.0

# Mineflayer 集成 (需要 Node.js 环境)
javascript>=1.1.0

# LLM (OpenAI Compatible API - 通义千问)
openai>=1.0.0
dashscope>=1.25.0  # 阿里云 DashScope SDK (用于 Embedding)

# Database (PostgreSQL + Async ORM)
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.14.0
greenlet>=3.0.0
