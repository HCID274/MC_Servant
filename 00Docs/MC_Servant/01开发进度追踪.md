# MC_Servant 开发进度追踪

> **项目阶段**: Week 9-10 规划中（Step 2：Actor → Resolver → Actions）  
> **最后更新**: 2026-01-04

---

## ✅ 已完成 (Week 1-4)

### 🔴 第1-2周: 骨架与通信

> **核心目标**: 打通 Java Plugin ↔ Python FastAPI ↔ Mineflayer Bot 的链路  
> **验收标准**: 玩家输入 `/servant hello`，Bot 跳一下并回复 "Ciallo~~~~"

#### Week 1: 环境搭建 + Java插件骨架

- [x] **安装服务器插件**
  - [x] FAWE ✅（FAWE性能更好）
  - [x] DecentHolograms ✅
  - [x] CommandAPI ✅ (命令框架，用于简化命令注册、参数校验、自动补全)
  - [x] Vault + EssentialsX ✅（经济系统）

- [x] **创建 Java 插件项目**
  - [x] Maven 项目 + pom.xml
  - [x] 添加依赖: OkHttp, CommandAPI, FastJSON2
  - [x] 插件主类 MCServant.java
  - [x] 基础命令 `/servant hello`

- [x] **学习 VillagerAgent**
  - [x] 跑通 example.py
  - [x] 理解 Mineflayer Bot 连接流程

- [x] **配置 Bot 女仆皮肤**
  - [x] 获取高清女仆皮肤文件 (推荐使用双层皮肤增加立体感)
  - [x] 使用 SkinsRestorer 配置 Bot 皮肤
  - [x] 验证 Bot 登录后皮肤正确显示

#### Week 2: WebSocket通信 + 端到端验证

- [x] **Python 后端骨架** ✅ (2025-12-29)
  - [x] FastAPI + WebSocket 服务器 (`backend/main.py`)
  - [x] 基础消息协议定义 (`backend/protocol.py` - pydantic 模型)
  - [x] 配置管理 (`backend/config.py`)
  - [x] WebSocket 连接管理器 (`backend/websocket/connection_manager.py`)
  - [x] 消息处理器 (`backend/websocket/handlers.py`)

- [x] **Java WebSocket 客户端** ✅ (2025-12-29)
  - [x] OkHttp WebSocket 封装 (`websocket/WSClient.java` - 心跳+自动重连)
  - [x] 接口抽象 (`websocket/IWebSocketClient.java`)
  - [x] 消息处理器 (`websocket/MessageHandler.java` - FastJSON2)
  - [x] 更新 MCServant.java 生命周期管理
  - [x] 更新 ServantCommands.java 发送 WebSocket 消息
  - [x] 插件编译成功: `MC_Servant-1.0.0.jar` (5MB)

- [x] **Mineflayer Bot 控制** ✅ (2025-12-29)
  - [x] Bot 控制接口 (`backend/bot/interfaces.py` - IBotController)
  - [x] Mineflayer 实现 (`backend/bot/mineflayer_adapter.py` - 独立实现，不依赖 VillagerAgent)
  - [x] 集成到 WebSocket Server

- [x] **🎯 端到端验证** ✅ (2025-12-30 完成版本迁移验证)
  - [x] 安装 Python 依赖 + Node.js mineflayer
  - [x] 部署插件到服务器
  - [x] `/servant hello` → Python → Bot 跳跃 + 回复 "Ciallo~~~~" ✅
  - [x] 版本迁移 1.19.2 → 1.20.6 验证通过 ✅

---

### 🟡 第3-4周: 接入 LLM 与意图识别

> **核心目标**: 大脑上线，LLM 理解玩家意图  
> **验收标准**: 玩家说"盖个房子" → 识别为 BUILD 意图

#### Week 3: LLM 接入 + 意图识别 + 状态机

- [x] **接入通义千问** ✅ (2025-12-31)
  - [x] API 封装 (`llm/qwen_client.py` - OpenAI 兼容接口)
  - [x] 抽象接口设计 (`llm/interfaces.py` - ILLMClient)
  - [x] 配置管理 (`config.py` 扩展 + `.env.example`)

- [x] **意图识别 Prompt** ✅ (2025-12-31)
  - [x] 设计意图分类体系 (`llm/intent.py` - Intent 枚举)
  - [x] 编写意图识别 System Prompt (INTENT_SYSTEM_PROMPT)
  - [x] 测试各种用户输入 (7/7 测试通过)

#### Week 4: 对话管理 + 分层记忆系统 (核心) ✅ (2026-01-01)

> **核心目标**: 实现基于信息熵压缩的无限层级递归记忆系统 (L0/L1/L2)
> **技术架构**: PostgreSQL + SQLAlchemy (Async) + LruCache + LLM Compression

- [x] **基础设施 (Phase 1-2)**
  - [x] PostgreSQL 数据库环境搭建 (Windows)
  - [x] 异步 ORM 层 (`db/models.py`, `context_repository.py`)
  - [x] 三层记忆模型设计 (L0工作记忆/L1情景记忆/L2核心记忆)

- [x] **记忆流逻辑 (Phase 3)**
  - [x] `ContextManager` (LRU缓存 + 细粒度锁 + 异步压缩队列)
  - [x] L0→L1 叙事化压缩 Prompt (第三人称叙述)
  - [x] L1→L2 认知化压缩 Prompt (用户画像/事实/情感)

- [x] **业务集成 (Phase 4)**
  - [x] Bot 个性化注入 (人格 + L2核心记忆 System Prompt)
  - [x] `IPersonalityProvider` 抽象接口与依赖注入
  - [x] 跨会话持久化 (重启后从 DB 完美复原)
  - [x] 集成到 WebSocket 消息流 (`handlers.py`)

- [x] **系统验证 (Phase 5)**
  - [x] 200+ 轮长对话测试脚本 (`test_long_conversation.py`)
  - [x] 关键事实保留验证 (通过 6/6 核心测试点)
  - [x] 验证 L0→L1→L2 压缩链完整性

- [x] **闲聊功能** ✅ (2025-12-31)
  - [x] 闲聊 Prompt 设计 (`_generate_chat_response`)
  - [x] NPC 人格设定 (猫娘助手，每句话结尾加喵~)

- [x] **头顶全息显示** ✅ (2026-01-01)
  - [x] DecentHolograms API 封装 (`IHologramService`, `HologramManager`)
  - [x] 显示当前状态/正在说的话 (双行: 身份 + 状态)
  - [x] 线程安全 (Bukkit.getScheduler().runTask)
  - [x] 2-tick 跟随频率，Y+2.9 高度偏移 (优化布局 Chat-above-ID)
  - [x] **分段展示**: 长文本自动语义切分，每段展示 5 秒 (startChatSegments)
  - [x] **状态缓存**: 说话结束后自动恢复之前的状态 (如"待命中")

- [x] **交互系统升级** ✅ (2026-01-01)
  - [x] 扩展 `/servant claim|release|list` 命令 (Java Plugin)
  - [x] 实现右键 GUI 管理菜单 (Inventory GUI)
  - [x] **优化 GUI 交互**: 移除重复按钮，使用 Adventure API 实现点击消息预填命令
  - [x] **统一命令格式**: 采用 `/svs @name <消息>` 和 `/servant @name <action>` (支持省略 @name)
  - [x] Python 后端 `servant_command` 消息处理 (ServantCommandHandler)
  - [x] 修复所有权校验逻辑 (兼容 UUID/玩家名双重匹配)
  - [x] 实现 `status` 命令的后端处理
  - [x] DecentHolograms 状态同步

- [x] **Bot 生命周期管理 (Phase 1-3)** ✅ (2026-01-02)
  - [x] **Init Sync 机制**: Java 启动即同步在线玩家并模拟 Join 事件，解决 Python 重启后的状态丢失问题
  - [x] **智能生命周期**: 主人上线 Bot 自动跟随上线，主人下线启动 10 小时倒计时
  - [x] **稳定性增强**: 登录指数退避重试 (5s → 80s)，超时优雅告别并下线
  - [x] **全息显示优化**: 修复无主状态下全息显示丢失问题，支持多行自动换行显示
  - [x] **名牌隐藏**: 自动隐藏 Bot 原版名牌，仅显示全息，优化视觉体验

- [x] **数据库迁移与深度集成 (Phase 4)** ✅ (2026-01-02)
  - [x] **Alembic 迁移**: 引入 Alembic 管理数据库版本，完成 `players` 和 `bots` 表结构升级
  - [x] **AuthMe 深度集成**: Java 监听 `LoginEvent`/`LogoutEvent`，实现仅同步已认证玩家
  - [x] **双向状态同步**: Python ↔ Java 实时同步玩家在线状态 (`is_online`, `last_login`)
  - [x] **上下文持久化修复**: 修复 `ContextManager` 集成 Bug，确保对话记录正确入库
  - [x] **时区统一**: 全局统一使用 Beijing Time (UTC+8) 解决时序错乱问题
  - [x] **Bot 数据同步**: 启动时自动同步 Bot 信息，`claim`/`release` 命令直连数据库
  - [x] **Owner 同步修复**: `Init Sync` 包含 owner 信息，解决 Python 重启后 Java 端缓存丢失问题
  - [x] **全局变量初始化修复**: 修复 `bot_repo` 和 `player_repo` 在 `lifespan` 函数中未正确声明为全局变量的问题

- [x] **交互系统升级 (Phase 5)** ✅ (2026-01-02)
  - [x] **右键菜单重构**: 根据所有权状态智能分流 (ClaimMenuGUI / ServantMenuGUI)
  - [x] **认领系统**: 实现无主 Bot 右键认领，双向同步所有权状态
  - [x] **全息交互**: 聊天时全息显示分段内容，闲聊/指令反馈更自然

---

## 🔄 进行中 (Week 5-6): Bot 动作 API 扩展

> **核心目标**: 让 Bot "动起来"，实现移动、采集、建造能力  
> **验收标准**: Bot 能够自动寻路到指定位置、采集指定方块
> **比喻**: 实现 Bot 的 **"手"**

### Phase 1: Mineflayer 插件集成 ✅ (2026-01-03)

- [x] **安装 npm 依赖**
  - [x] `mineflayer-collectblock`, `mineflayer-tool`, `minecraft-data`

- [x] **实现 IBotActions 接口** (`backend/bot/interfaces.py`)
  - [x] `ActionResult`, `ActionStatus`, `IBotActions`

- [x] **实现 MineflayerActions 类** (`backend/bot/actions.py`)
  - [x] `goto(target)` - ✅ 导航可用
  - [x] `mine(block_type, count)` - ✅ 采集成功
  - [x] `craft(item_name, count)` - ✅ 精确配方可用 (tag配方受限)
  - [x] `scan(target_type, radius)` - ✅ 扫描可用
  - [x] `get_state()` - ✅ 状态可用
  - [x] `give(player, item, count)` - 交付物品
  - [x] `equip(item_name)` - 装备物品

- [x] **Tag 配方解析增强** (2026-01-03)
  - [x] 创建 `data/tag_recipes.json` (8 个 Tag 组: planks, logs, wool 等)
  - [x] 实现 `bot/tag_resolver.py` (`ITagResolver` 接口 + 反向索引)
  - [x] 增强 `craft()` 方法 (Tag-aware 材料检查)

> **✅ Known Issue 已解决**: Tag 配方问题通过 `TagResolver` + `_find_executable_recipe()` 解决

---

## ⏳ 待开始 (Week 7-12)

### 🟢 Week 7-8: 任务执行框架 + 神经符号语义解析

> **核心目标**: 实现栈式任务规划器 + 神经符号语义解析系统  
> **比喻**: 实现 Bot 的 **"眼"** (感知) 和 **"脑"** (规划)

#### 栈式任务规划器 (Stack-Based Planner) ✅ (2026-01-03)

- [x] **实现 StackPlanner** (`backend/task/stack_planner.py`)
  - [x] `StackTask` 数据结构 (`interfaces.py`)
  - [x] `push()` / `pop()` / `current()` 方法
  - [x] 任务阻塞与恢复逻辑 (IN_PROGRESS/BLOCKED 状态管理)
  - [x] 软限制(6)和硬限制(9)防止无限递归
  - [x] 栈追踪调试功能

- [x] **实现 TaskExecutor** (`backend/task/executor.py`)
  - [x] 执行循环 (规划 → 执行 → 反思)
  - [x] 错误处理 + 重试机制 (max_retries)
  - [x] 失败时自动压栈子任务 (`_handle_task_failure`)
  - [x] 修复 replan 返回空计划时的 Bug (LLM 放弃检测)

- [x] **实现 LLMTaskPlanner** (`backend/task/llm_planner.py`)
  - [x] 规划 Prompt 设计 (PLAN_SYSTEM_PROMPT)
  - [x] JSON 结构化输出格式
  - [x] 重规划逻辑 (REPLAN_SYSTEM_PROMPT)

- [x] **实现 PrerequisiteResolver** (`backend/task/prerequisite_resolver.py`)
  - [x] 符号层前置任务解析 (Fast Path)
  - [x] 处理 `INSUFFICIENT_MATERIALS` 和 `NO_TOOL` 错误码
  - [x] 规则库 (`data/prerequisite_rules.json`) - 可合成物品/可采集方块/可冶炼物品/Tag等价组

- [x] **完整测试套件** (`backend/tests/`)
  - [x] `test_stack_planner.py` (14个测试，全部通过)
  - [x] `test_task_executor.py` (9个测试，全部通过)
  - [x] Mock 设计优化 (可编程的演员，支持 replan_steps=None 模拟 LLM 放弃)

- [x] **Bug修复与稳定性改进** ✅ (2026-01-03)
  - [x] **WorkingState**: 修复同步 `queue_event` 导致状态机卡死的问题 (改为异步)
  - [x] **PermissionGate**: 修复内部事件 (`PLANNING_COMPLETE`) 默认被权限门拦截的问题
  - [x] **JSPyBridge**: 修复 `players.get()` 在 JS Proxy 对象上的兼容性崩溃
  - [x] **MineflayerActions**:修复 `give` 命令丢弃整个物品栈的问题 (改用 `toss` + 数量)
  - [x] **MineflayerActions**: 修复 `give` 命令丢弃位置不准 (增加 `lookAt` 实时面向玩家)
  - [x] **LLMTaskPlanner**: 修复 `replan` 响应解析错误 (增加 `JSONDecodeError` 显式处理)
  - [x] **TaskExecutor**: 修复 `scan` 动作不支持 `timeout` 参数导致执行失败的问题
  - [x] **LLMContext**: 修复 `give` 命令 LLM 使用占位符问题 (注入 `owner_name`)

#### 神经符号语义解析系统 (Neuro-Symbolic Resolver) ✅ (2026-01-03)

- [x] **构建静态知识库**
  - [x] 编写 `scripts/build_knowledge_base.py` (2308行，完整实现)
  - [x] 从 minecraft-data 提取所有物品/方块 ID
  - [x] 用 LLM 批量生成语义标签 (可选，支持 `--use-llm`)
  - [x] 生成 `backend/data/mc_knowledge_base.json` (71个标签，923个物品)
  - [x] **核心组件**:
    - [x] `MinecraftDataSource` - 从 npm minecraft-data 提取游戏数据
    - [x] `RegexTagGenerator` - 基于正则的标签生成器 (覆盖 100% 物品)
    - [x] `LLMTagGenerator` - 基于 LLM 的补全分类器 (带缓存)
    - [x] `KnowledgeBaseBuilder` - 构建器主流程 (依赖抽象接口)
  - [x] **高级特性**:
    - [x] 聚合标签 (weapons = swords + bows + tridents)
    - [x] 中英文别名映射 (约 2000 个别名: "木头" → "logs")
    - [x] 审核报告自动生成 (Markdown 格式)
    - [x] 缓存机制 (避免重复调用 LLM)

- [x] **实现 EntityResolver** (`backend/perception/resolver.py`) ✅ (2026-01-03)
  - [x] `get_candidates(concept)` - 语义→候选ID列表
  - [x] `validate_ids(ids)` - 校验ID合法性
  - [x] `resolve(concept, llm_candidates)` - 候选集 ∩ 环境感知
  - [x] 渐进式搜索 (32格 → 64格 → 反馈)
  - [x] **核心模块**:
    - [x] `perception/interfaces.py` - IKnowledgeBase, IScanner, IInventoryProvider
    - [x] `perception/knowledge_base.py` - JsonKnowledgeBase (单例模式)
    - [x] `perception/scanner.py` - MineflayerScanner + MockScanner
    - [x] `perception/inventory.py` - BotInventoryProvider + MockInventoryProvider
    - [x] `perception/resolver.py` - EntityResolver 主类
  - [x] **设计决策**:
    - [x] KB 绝对优先，LLM 仅补位 (确定性 > 概率性)
    - [x] ResolveStatus 枚举区分错误类型 (UNKNOWN_CONCEPT, NOT_FOUND_IN_WORLD 等)
    - [x] IScanner: async, IKnowledgeBase/IInventoryProvider: sync
    - [x] Block 坐标转中心点 (x+0.5, y, z+0.5)
  - [x] **单元测试**: 19 个测试全部通过

- [x] **集成到状态机 WORKING 状态** ✅ (2026-01-03)
  - [x] 创建 `BotContext` 类 (DI 容器 + 节流方法)
  - [x] 添加 `TASK_STOP`, `PLANNING_COMPLETE` 事件类型
  - [x] 重构 `PlanningState` (Auto-Start + 后台任务)
  - [x] 重构 `WorkingState` (TaskExecutor 后台执行)
  - [x] 更新 `IdleState` 传递 BotContext
  - [x] `StateMachine.process_pending_events()` 处理内部事件队列
  - [x] `main.py` 完整依赖图: `MineflayerActions` → `TaskExecutor` → `BotContext`
  - [x] 所有测试 (23个) 保持通过

#### ⭐ Week 7-8 补充：采集闭环（Tick Loop）与鲁棒性强化 ✅ (2026-01-03)

> **目标**: 先从“挖矿/砍树（资源采集）”切入验证感知-决策闭环，优先保证不崩溃/不卡死

- [x] **Tick Loop 执行模式（采集类任务）** ✅
  - [x] `TaskExecutor` 对采集任务启用 Observe→Act→Execute→Reflect（每 tick 只执行 1 个动作）
  - [x] `LLMTaskPlanner.act()` 短提示词：每次只产出一步（或 done=true）
  - [x] `task_type` 透传到 executor（仅对 mine 启用 tick loop）

- [x] **完成判据：Inventory Delta** ✅
  - [x] 采集任务不再依赖 LLM 主动宣告 done
  - [x] 达到背包增量阈值自动收敛完成

- [x] **混合参照系 + 指代语锚定** ✅
  - [x] “我这边/离我最近”强锚定 `owner_position`
  - [x] 扫描不到目标且 `distance(bot, owner) > 50` 时触发隐式 `goto(owner_position)`

- [x] **规则库（Rulebook）起步** ✅
  - [x] 新增 `backend/data/behavior_rules.json`（阈值/关键词/隐式子任务/恢复策略骨架）
  - [x] 新增 `backend/task/behavior_rules.py` 并在 `TaskExecutor` 中消费关键阈值（配置驱动，避免写死）

- [x] **采集动作稳定性修复** ✅
  - [x] `mine_tree`：避免跨桥传 Python callback 导致 Node uncaughtException
  - [x] `mine_tree`：pathfinder 决策超时自动重试（每 log 多次尝试 + stop/靠近重试）
  - [x] `mine_tree`：BFS 连通原木识别，避免两棵树相互“串联”

- [x] **实时 owner_position 贯通** ✅
  - [x] Java plugin `player_message` 附带实时坐标（player_x/y/z）
  - [x] Python 协议/handlers/state/executor 链路透传并优先使用该坐标

- [x] **工程稳定性修复** ✅
  - [x] 默认关闭后端热重载（避免任务执行中被 watchfiles reload 打断）
  - [x] 修复 Python 3.8 兼容性：`str | Path` → `Union[str, Path]`

---

### ⭐ Week 9-10: Step 2 正统架构（Actor → Resolver → Actions）

> **核心目标**: 让采集任务从“LLM 直接给 block_type/坐标”升级为“LLM 只给语义参数，由 Resolver 落地”，并将澄清提问纳入闭环

#### A. 任务层：粗粒度 Decompose（Gather 作为 StackTask） ✅ (2026-01-04)

- [x] **基础设施升级**
  - [x] `TaskType` 枚举 (GATHER, CRAFT, BUILD, GOTO, GIVE, COMBAT, FOLLOW)
  - [x] `ITaskDecomposer` / `ITaskRunner` 接口 (策略模式)
  - [x] `RunnerRegistry` (TaskType → Runner 映射)

- [x] **Runner 实现**
  - [x] `GatherRunner` (封装原 `_execute_task_tick_loop`，Tick Loop 模式)
  - [x] `LinearPlanRunner` (封装原线性执行逻辑)

- [x] **Executor 重构**
  - [x] `execute_tasks()` 入口 (支持 Decomposer 产出列表)
  - [x] `_execute_task` 接入 RunnerRegistry (优先策略派发，兼容旧逻辑)
  - [x] `StackTask` 新增 `task_type` 字段

- [x] **LLM Decomposer**
  - [x] `LLMTaskDecomposer` (用户意图 → 粗粒度 StackTask 列表)
  - [x] Prompt 模板设计 (只做逻辑分解，不展开资源依赖)

#### B. 决策层：Actor 只输出语义参数（不出坐标）

- [ ] **Actor 输出协议升级**
  - [ ] `{"action":"mine","target":"nearest_log"}` / `{"action":"mine_tree","target":"nearest_tree"}`
  - [ ] 增加 `{"action":"clarify","question":"...","choices":[...]}`（懒惰澄清：默认值优先，仅在无法兜底时提问）

#### C. 符号层：Resolver 负责落地（Concept 优先但兼容 Item）

- [ ] **Resolver Grounding 集成到 Tick Loop**
  - [ ] Concept（如 `logs`）→ KB 展开候选 → 扫描 → 锁定环境中存在的具体 log
  - [ ] Item（如 `oak_log`）→ 精确匹配
  - [ ] 输出落地参数：`near_position/search_radius/target_coords`

#### D. 规则库消费：从“骨架”到“可控行为”

- [ ] **behavior_rules.json 驱动更多策略**
  - [ ] owner_fallback / search_radius / retry 策略统一配置
  - [ ] error_code → recovery_strategy 映射接入（分级恢复 L1/L2/L3/L4）

#### E. 测试与可观测性

- [ ] **采集闭环集成测试**
  - [ ] “把我这边的树砍了”/“弄点木头”/“附近没有木头怎么办” 三类用例
  - [ ] 关键日志：每 tick 的 Observe/决策/落地结果/Inventory Delta 收敛

---

### Week 11-12: （远期/可选）多智能体协作 + 优化演示

> **核心目标**: 在 Step2 稳定后，再评估是否引入 TeamLeader + Blackboard（当前不作为主线）

- [ ] **TeamLeader Agent** (`backend/agent/team_leader.py`)
  - [ ] 任务分解逻辑
  - [ ] Bot 分配策略

- [ ] **Blackboard 模式** (`backend/agent/blackboard.py`)
  - [ ] 需求/供给信息共享
  - [ ] 位置共享
  - [ ] 匹配算法

- [ ] **多任务测试**
  - [ ] 建造任务测试
  - [ ] 挖矿任务测试
  - [ ] 种田任务测试

- [ ] **成功率统计报告**
- [ ] **录制演示视频**
- [ ] **撰写技术博客/简历项目描述**

---

### 🔵 (可选) 建筑系统

> 如有余力，后续补充

- [ ] **建筑规划 Prompt**
  - [ ] 整理建筑风格词汇表
  - [ ] 设计建筑规划 System Prompt
  - [ ] LLM 输出结构化建筑方案

- [ ] **蓝图数据结构**
  - [ ] 文本描述 → 结构化 JSON
  - [ ] 对接 .litematic 模板库

- [ ] **litemapy 集成**
  - [ ] 解析 .litematic 文件
  - [ ] 提取方块坐标数据

- [ ] **WorldEdit 预览**
  - [ ] Java 调用 WorldEdit API
  - [ ] 显示建筑轮廓/地基

---

*最后更新: 2026-01-04（粗粒度 Decompose + 策略模式重构已落地）*
